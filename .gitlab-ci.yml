stages:
  - build
  - test

create_file:
  image: alpine
  stage: build
  script:
    - echo "Creating a file ..."
    - mkdir build
    - touch build/test.txt
  artifacts:
    paths:
      - build/

test_file:
  image: alpine
  stage: test
  script:
    - test -f build/test.txt